# 最小测试步骤

严格按顺序，**任一步失败就停下排查**，不要跳到下一步。

## 准备
- [ ] 已按 `feishu/setup.md` 创建飞书应用并发布
- [ ] 已建两张表，并在「社群配置」表录好 19 个群名 + 规模
- [ ] 已部署 EdgeOne 函数并配齐 6 个环境变量
- [ ] 拿到看板访问域名（EdgeOne 提供）

## 测试 1：后端读取
- [ ] 浏览器/终端访问 `<域名>/api/data`
- [ ] 应返回 JSON：`{ "updatedAt": "...", "groups": [ ...19个群 ], "records": {} }`
- [ ] 若报错：查 EdgeOne 函数日志 + 环境变量是否正确 + 飞书应用是否已发布

## 测试 2：网页读取
- [ ] 手机微信打开看板域名
- [ ] 看板能显示 19 个群（数据来自飞书）
- [ ] 顶部**不显示**「离线数据」提示

## 测试 3：保存一条测试数据
- [ ] 每日填写页填几个群的数字
- [ ] 首次保存弹出密码框 → 输入 `REDPET_WRITE_PASSWORD`
- [ ] 提示「✓ 已保存到面板」

## 测试 4：换设备读取
- [ ] 换一台手机 / 电脑打开同一域名
- [ ] 能读到刚才保存的数据 → **通过 ✅**

## 测试 5：迁移历史数据
- [ ] 测试全部通过后，再把旧 `docs/data/daily.json` 的历史记录导入飞书
- [ ] 导入方式：在飞书表里手动补录，或用飞书 Open API 脚本批量写入
- [ ] 导入后 `/api/data` 的 `records` 包含历史

## 回滚
- 飞书不可用时，看板自动降级为「离线数据」（内嵌备份 + 本机缓存），不影响查看；保存失败可用「导出备份」存本地 JSON。
