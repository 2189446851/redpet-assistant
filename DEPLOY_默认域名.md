# 平台默认域名部署步骤（免备案 · 免运维）

> 目标：拿到一个 `https://xxx.edgeone.site` 域名，手机微信能打开看板、能保存数据到飞书。
> 全程约 40 分钟。先做完飞书，再做 EdgeOne（因为部署时要填飞书的参数）。

---

## 总览

| 顺序 | 做什么 | 产出 |
|---|---|---|
| ① | 飞书：建应用 + 建两张表 | 拿到 5 个参数 |
| ② | EdgeOne：导入 GitHub 仓库 + 配 6 个环境变量 | 拿到默认域名 |
| ③ | 测试（按 MINIMAL_TEST.md 6 步） | 确认能用 |

---

## ① 飞书侧（约 20 分钟）

### 1.1 创建企业自建应用
1. 打开 **https://open.feishu.cn/app** ，用**公司飞书账号**登录。
   - 若提示"无权限/不是开发者" → 找 IT 或飞书管理员给你开**开发者**权限。
2. 点「创建企业自建应用」→ 名称填 `REDPET看板数据` → 创建。
3. 进入应用 → 左侧「凭证与基础信息」→ 复制 **App ID** 和 **App Secret**，先粘到记事本。
4. 左侧「权限管理」→ 搜索 `bitable` → 勾选 **「查看、评论、编辑和管理多维表格」(bitable:app)**。
5. 左侧「版本管理与发布」→ 创建版本（版本号 `1.0.0`）→ 保存 → **申请发布**。
   - 若需管理员审批，请管理员通过。**不发布的话接口调不通。**
6. 左侧「可用范围」→ 把你自己和需要填数的人加进去。

### 1.2 建多维表格的两张表
1. 打开飞书（网页 **https://www.feishu.cn** 或客户端）→ 新建「多维表格」，命名 `REDPET社群数据`。
2. **记下 app_token**：看地址栏，形如 `.../base/appXXXXXXXX` 或 `bitable.feishu.cn/appXXXXXXXX`，其中 **`appXXXXXXXX` 就是 `FEISHU_BITABLE_APP_TOKEN`**。
3. **表1「每日活跃数据」**，建这些字段（一行 = 一天）：

   | 字段 | 类型 |
   |---|---|
   | 日期 | 日期 |
   | 下面 19 个群名，每个一列 | **数字** |
   | 更新时间 | 日期时间 |
   | 填写人 | 文本 |

   19 个群名照 `feishu/建表_群名列清单.md` 建，**列名必须一字不差**（差一个字保存就会报"未知群名"）。

4. **表2「社群配置」**（在同一多维表格里再建一个数据表）：

   | 字段 | 类型 |
   |---|---|
   | 群名 | 文本 |
   | 规模 | 数字 |

   录入 19 行（群名 + 规模，规模缺失的留空）。

5. **记下两个 table_id**：分别点开两张表，看地址栏里 `table=tblXXXXXX`，**`tblXXXXXX` 就是 table_id**。
   - 每日活跃数据表 → `FEISHU_TABLE_ID`
   - 社群配置表 → `FEISHU_GROUPS_TABLE_ID`

### 1.3 定一个内部保存密码
自己想一个（如 `Redpet@2026`），这就是 `REDPET_WRITE_PASSWORD`，**只私下发给填写的人**，不要写进群公告。

> 到这里你应该有 5 个飞书参数：App ID、App Secret、app_token、两个 table_id，外加你自己定的密码。

---

## ② EdgeOne 侧（约 15 分钟）

### 2.1 建项目（用 Git 导入，最省事）
1. 打开 **https://console.cloud.tencent.com/edgeone** ，用腾讯云账号登录（**尽量用公司账号**；暂时没有就先用个人账号跑通，后续迁到公司账号）。
2. 找到 **EdgeOne Pages / Makers** → 新建项目 → 选择「**导入 Git 仓库**」。
3. 授权 GitHub → 选仓库 `2189446851/redpet-assistant` → 分支 `main`。
4. 构建设置：
   - 框架预设：选「其他 / 静态」
   - 构建命令：**留空**
   - 输出目录：填 `docs` 或留空都可以（仓库根目录和 `docs/` 下都放了 index.html）
   - **函数目录必须是 `edge-functions`**（这个别改）
5. 点创建 → 等待构建完成 → 得到默认域名，形如 **`https://xxx.edgeone.site`**。

> 这个域名是平台自有、**已经备过案**的，所以你不用买域名、不用备案，直接能用。

### 2.2 配环境变量（密钥只放这里，前端永远看不到）
项目 →「环境变量」（或 设置 → 环境变量）→ 添加这 6 个：

| 变量名 | 填什么 |
|---|---|
| `FEISHU_APP_ID` | 飞书应用的 App ID |
| `FEISHU_APP_SECRET` | 飞书应用的 App Secret |
| `FEISHU_BITABLE_APP_TOKEN` | 多维表格的 appXXXXXXXX |
| `FEISHU_TABLE_ID` | 每日活跃数据表的 tblXXXXXX |
| `FEISHU_GROUPS_TABLE_ID` | 社群配置表的 tblXXXXXX |
| `REDPET_WRITE_PASSWORD` | 你自己定的内部密码 |

**保存后要重新部署一次**，环境变量才会生效。

---

## ③ 测试（按 MINIMAL_TEST.md）

1. 浏览器打开 `https://你的域名/api/data` → 应返回一段 JSON，里面有 19 个 `groups`。
   - 打不开或报错 → 多半是环境变量填错，或飞书应用没发布。去 EdgeOne 看函数日志。
2. 手机微信打开 `https://你的域名` → 看板显示 19 个群，**不**显示"离线数据"。
3. 进「每日填写」→ 填几个群的数字 → 保存 → 首次会弹密码框 → 输你定的密码 → 出现「✓ 已保存到面板」。
4. **换一台手机或电脑**打开同一域名 → 能读到刚才存的数据 → **通过**。
5. 全部通过后，再把旧 `docs/data/daily.json` 的历史数据导入飞书（手动补录或脚本批量写）。

---

## 常见坑

| 现象 | 原因 |
|---|---|
| `/api/data` 报错 / 返回 error | 环境变量漏填或填错；飞书应用未发布 |
| 保存报「未知群名」 | 飞书表列名与群名清单不一致（多了空格、少了个字） |
| 保存报「密码错误」 | 输入的密码 ≠ `REDPET_WRITE_PASSWORD` |
| 页面一直显示「离线数据」 | 后端不通，先直接测 `/api/data` |
| 飞书应用建不了 | 没有开发者权限，找管理员开 |

---

## 关于域名（后续再说）

- 默认域名先用着，**不用备案、不用运维**。
- 若公司要正式化：买域名 → ICP 备案（10–20 个工作日）→ 在 EdgeOne 绑定自定义域名。这步不影响你现在先用起来。
