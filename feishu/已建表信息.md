# 飞书表已建好 · 关键信息（不含密钥）

> 建表时间：2026-09-20
> 本文件只记录 ID，**不记录 App Secret 和保存密码**（它们只存在于 EdgeOne 环境变量里）。

## 三个关键 ID（填 EdgeOne 环境变量用）

| 环境变量名 | 值 |
|---|---|
| `FEISHU_APP_ID` | `cli_aa254f9742781ced` |
| `FEISHU_APP_SECRET` | **不记录** —— 去飞书开放平台「凭证与基础信息」复制 |
| `FEISHU_BITABLE_APP_TOKEN` | `QMU1bBDf8ayzwxs96M3cuZ9YnFh` |
| `FEISHU_TABLE_ID`（每日活跃数据） | `tblydLRMjpKHOL4J` |
| `FEISHU_GROUPS_TABLE_ID`（社群配置） | `tbl8EFrQx60qNStT` |
| `REDPET_WRITE_PASSWORD` | **不记录** —— 自己定的内部保存密码 |

## 多维表格位置

- 表格名：**REDPET社群数据**（由脚本自动创建）
- 打开方式：飞书 → 云文档 → 搜索「REDPET社群数据」
- 直链格式：`https://QMU1bBDf8ayzwxs96M3cuZ9YnFh.feishu.cn` 或在飞书云文档里搜名字

## 表结构（已验证）

**表1「每日活跃数据」** —— 23 个字段：
- `日期`（日期）
- 19 个群名（数字）：猫猫托儿所1群 … 萌宠美颜社，列名与原清单一致
- `更新时间`（日期）、`填写人`（文本）
- `多行文本`：飞书建表时自带的默认列，**脚本没能删掉**。它是文本类型，后端只读数字列，所以**不影响任何功能**；想干净可以手动在飞书里删掉这一列。

**表2「社群配置」** —— 19 行已写入：
- 群名 / 规模，与 `建表_群名列清单.md` 一致（原本规模缺失的 4 个群填的是 0，可在飞书里改成实际人数）

## 交接备注

- 管理员：请在飞书开放平台把该应用的**管理员**加上至少两名公司正式员工；多维表格也给他们「可管理」权限。
- App Secret 若曾外泄过，在开放平台点「重新生成」即可换一把（换完记得同步更新 EdgeOne 环境变量）。
