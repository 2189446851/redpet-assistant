# REDPET 看板 · 交接文档

## 一句话架构
网页（`index.html`）只和**同域的一个小后端接口**说话；这个小接口替网页保管飞书密钥，去读写飞书多维表格。

## ⚠️ 最关键的一句话
**不要让网页直接连接飞书，因为那样飞书密码会被写进网页；中间必须有一个小接口替网页保管密码。**

## 管理员（须添加至少两名公司正式员工）
- 飞书开放平台：该自建应用的「管理员/开发者」
- 飞书多维表格：该表格的「可管理」权限
- 腾讯云 / EdgeOne：该 Makers 项目的「项目管理员」
> 全部用公司账号创建，不要使用实习生个人账号。

## 飞书表位置
- 多维表格 app_token：`（部署时填入）`
- 表1 每日活跃数据 table_id：`（部署时填入）`
- 表2 社群配置 table_id：`（部署时填入）`

## 环境变量位置（密钥只在这里）
EdgeOne Makers 控制台 → 项目 → 环境变量：
`FEISHU_APP_ID` / `FEISHU_APP_SECRET` / `FEISHU_BITABLE_APP_TOKEN` / `FEISHU_TABLE_ID` / `FEISHU_GROUPS_TABLE_ID` / `REDPET_WRITE_PASSWORD`

## 部署方式
- 代码在 GitHub 仓库 `redpet-assistant`（仅代码备份，不再存每日数据）。
- 推到 main 后 EdgeOne 自动 CI 构建；或本地 `edgeone makers deploy`。
- 飞书密钥不在仓库里，部署不会泄露。

## 导出备份
- 看板「导出备份」按钮：下载当前数据 JSON 到本机（断网时兜底）。
- 飞书多维表格本身就是正式数据源与备份；可定期复制一份副本表。

## 密码轮换
1. EdgeOne 改 `REDPET_WRITE_PASSWORD` 为新值。
2. 通知填写人：清除看板网页的本地保存密码（清除该站点数据，或在手机上重输新密码）。
3. 新密码只私下传递，不进代码、不进群公告明文。

## 故障排查
- 看板显示「离线数据」：后端/飞书不可达 → 查 EdgeOne 函数日志、环境变量、飞书应用是否已发布。
- 保存报「密码错误」：与 `REDPET_WRITE_PASSWORD` 不一致。
- 保存报「未知群名/人数超范围」：群名须与飞书表列名完全一致；人数须 0~2000 整数。
